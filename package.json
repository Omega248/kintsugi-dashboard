{
  "name": "kintsugi",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Kintsugi Motorworks dashboard and Discord bot — single unified Cloudflare Worker",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "test": "node test-rates.mjs && node test-preferences.cjs && node test-invoice-flow.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
